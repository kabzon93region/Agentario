# v0.14.93 — Исправление отображения токенов компакции

**Дата:** 2026-08-05

## Fixed

- **Корректное отображение токенов в сообщении о завершении компакции** — сообщение "✅ Компакция завершена" теперь показывает `chatTokens` (реально суммаризированные токены диалога) вместо `displayInputTokens` (весь контекст включая system prompt и tool schemas). Исправляет некорректное отображение "18,293 → 1,963" когда на суммаризацию отправлялось только ~6,859 токенов.
