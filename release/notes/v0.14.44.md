# Agentario v0.14.44

## Что нового

### Fixed

- Обзор проекта больше не толкает модель в `Get-ChildItem` (противоречие правил vs блокировка shell убрано).
- Старт обзора: только `git status`, затем `read_files` / `semantic_search`.

## Установка

1. Скачайте **`agentario-0.14.44.vsix`** из Assets релиза на GitHub.
2. VS Code → Extensions → `...` → **Install from VSIX**.
