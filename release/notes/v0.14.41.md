# Agentario v0.14.41

## Что нового

### Fixed

- Сборка `@agentario/core`: неиспользуемый импорт `MessageWithMetadata`; `DEFAULT_FILE_READ_OPTIONS` совместим с опциональным `summarizeLargeFile`.

Также включает 0.14.40 (саммари больших файлов + прогресс индексации).

## Установка

1. Скачайте **`agentario-0.14.41.vsix`** из Assets релиза на GitHub.
2. VS Code → Extensions → `...` → **Install from VSIX**.