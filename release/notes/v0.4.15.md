## Agentario 0.4.15

### Исправлено

- **Очистка индекса:** кнопка «Очистить» теперь реально удаляет индекс (исправлено подтверждение в webview VS Code).
- **Надёжность delete:** повторное удаление по hash, если индекс остался после первого прохода.

### Установка

`agentario-0.4.15.vsix` → VS Code: Extensions → Install from VSIX.
