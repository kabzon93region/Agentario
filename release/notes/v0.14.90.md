## [0.14.90] - 2026-08-04

### Fixed
- **Stats для ВСЕХ сообщений в UI** — `findApiStatsForMessage` теперь ищет stats backward (к предыдущему `api_req_started`), а не forward. Исправляет проблему, когда stats отображались только у последнего сообщения, а у промежуточных (tool, command, thinking) — нет. Теперь каждый tool/command/thinking получает stats от своего API-вызова
