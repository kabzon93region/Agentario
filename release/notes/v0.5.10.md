# Agentario v0.5.10

## Исправления

- **Пресеты:** устранена гонка, при которой первый клик на пресет LM Studio после Xiaomi показывал настройки предыдущего провайдера. Устаревшие `updateApiConfigurationPartial` / `commitModelSelection` от размонтируемого UI теперь полностью игнорируются в течение 2 с после применения пресета (раньше блокировался только повторный `postStateToWebview`, но бэкенд-состояние всё равно портилось).
