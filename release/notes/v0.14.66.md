## v0.14.66

### Добавлено
- Lab API: `/api/stop`, `/api/clear`, `/api/delete_task`

### Изменено
- `/api/new_task` по умолчанию: stop → delete → home → новый чат
- Корректный idle по turnPhase/sessionRunning
