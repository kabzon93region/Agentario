# Agentario v0.14.51 — UX: экспорт, статистика, скролл (Волна 3)

## Исправлено
- Экспорт чата: задание (task) теперь всегда в начале, а не в конце из-за разных систем timestamps.
- completion_result теперь показывает статистику токенов (ищется backward, а не только forward).

## Изменено
- Скроллбар в ленте чата виден (был скрыт через CSS).
