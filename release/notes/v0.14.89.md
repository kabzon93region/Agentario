# Agentario v0.14.89

## Что нового

### Fixed

- **Stats в экспорте: backward search** — `findStatsForMessage` теперь ищет stats backward (к предыдущему `api_req_started`), что правильно ассоциирует stats с API-вызовом, произведшим сообщение. Исправляет дублирование stats у consecutive tool-сообщений
