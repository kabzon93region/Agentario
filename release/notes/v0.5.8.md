# Agentario 0.5.8

## Исправлено

- Пресеты модели: UI больше не отправляет полную устаревшую `apiConfiguration` при изменении отдельных полей. Это устраняет сценарий, где первый клик по LM Studio пресету применялся на backend, но затем UI откатывал конфигурацию обратно на Xiaomi.
- Plan/Act: частичные изменения настроек больше не перезаписывают соседний режим старыми значениями.

Установка: `release/agentario-0.5.8.vsix`
