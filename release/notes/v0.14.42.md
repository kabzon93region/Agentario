# Agentario v0.14.42

## Что нового

### Fixed

- MCP после перезапуска и включения: очередь подключений в `McpHub` убирает гонки init/watcher/seed/toggle.
- Сессия ждёт готовности MCP перед снимком tools — инструменты доступны сразу.
- Seed MCP: атомарная запись, всегда reload после seed, починка `npx`/PATH на Windows, merge из legacy если primary пустой.

## Установка

1. Скачайте **`agentario-0.14.42.vsix`** из Assets релиза на GitHub.
2. VS Code → Extensions → `...` → **Install from VSIX**.

## Требования

- VS Code 1.84+
- LM Studio или Ollama — для локальных моделей
