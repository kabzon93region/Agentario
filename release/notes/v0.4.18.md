# Agentario v0.4.18

## Что нового

### Fixed

- **Правила (Rules):** создание файла через «+» сразу показывает его в списке и открывает в редакторе VS Code (без перезапуска IDE).
- Исправлены пути workspace-правил (`.agentariorules` вместо устаревшего `.clinerules`) и сканирование глобальной папки `Documents/Agentario/Rules`.

## Установка

Скачайте `agentario-0.4.18.vsix` из Assets и установите через **Extensions → Install from VSIX**.
