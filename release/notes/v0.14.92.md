# Agentario v0.14.92

## Что нового

### Fixed

- **Точность оценки токенов при компакции** — estimateToolResultLength теперь считает structured entries (ToolOperationResult) так же, как ormatStructuredToolEntry: извлекает query, 
esult (обрезанный до 2,000 chars) и error, а не считает полный JSON.stringify. Исправляет завышение оценки "на суммаризацию" в ~3 раза
- **Улучшены логи компакции** — добавлено пояснение "[до трансформации: файлы→стабы, thinking→2k]" в лог "Расчёт", лог units показывает разницу между оценкой и реальными токенами

## Установка

1. Скачайте **`agentario-0.14.92.vsix`** из Assets релиза на GitHub.
2. VS Code → Extensions → `...` → **Install from VSIX**.

## Требования

- VS Code 1.84+
- LM Studio или Ollama — для локальных моделей (см. [README](https://github.com/kabzon93region/Agentario#lm-studio-локальная-модель))
